import React from 'react';

const Blog = (props) => {
	return (
		<div className="container">
			Welcome to the blog
		</div>
	);
};


// React Engine needs exports, don't export default
module.exports = Blog;
